import React from "react"
import styled from "styled-components";
export const Navbar = () => {
  return (
    <Container className="col-3"></Container>
  );
};

const Container = styled.nav`
background: white;
height: "100vh
`